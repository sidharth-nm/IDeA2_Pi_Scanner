# IDeA2_Pi_Scanner
Scripts to be run on the Raspberry Pi to execute the scanning and 3D model generation processes, before sending to the n8n backend.

## Setup
Install `requests` and `python-dotenv` using pip; or, alternatively, run `setup.sh`.